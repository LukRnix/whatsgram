import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'de.whatsgram.app',
  appName: 'vite-template-solid',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
